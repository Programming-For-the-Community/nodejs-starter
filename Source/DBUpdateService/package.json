{
  "name": "db-update-service",
  "version": "1.0.0",
  "description": "Updates MongoDB based on queue messages",
  "main": "server.js",
  "scripts": {
    "build": "npm install",
    "start": "npm run build && node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Charlie Hahm",
  "license": "MIT",
  "dependencies": {
    "amqplib": "^0.10.3",
    "db-update-service": "file:",
    "express": "^4.19.2"
  }
}
